using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class 문자숫자변환: MonoBehaviour
{
    //문자 -> 숫자 
    private int two = int.Parse("1")+int.Parse("1");

    //숫자 -> 문자      
    .ToString()
    void Start()
    {
        Debug.Log(two);
        //결과는 2
    }
}
